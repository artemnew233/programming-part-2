cmake_minimum_required(VERSION 3.20)
project(task06 LANGUAGES CXX)
add_executable(task06 main.cpp)
target_compile_features(task06 PRIVATE cxx_std_20)
if(MSVC)
    target_compile_options(task06 PRIVATE /W4)
else()
    target_compile_options(task06 PRIVATE -Wall -Wextra -Wpedantic)
endif()
