cmake_minimum_required(VERSION 3.20)
project(task04 LANGUAGES CXX)
add_executable(task04 main.cpp)
target_compile_features(task04 PRIVATE cxx_std_20)
if(MSVC)
    target_compile_options(task04 PRIVATE /W4)
else()
    target_compile_options(task04 PRIVATE -Wall -Wextra -Wpedantic)
endif()
