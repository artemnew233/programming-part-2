cmake_minimum_required(VERSION 3.20)
project(task05 LANGUAGES CXX)
add_executable(task05 main.cpp)
target_compile_features(task05 PRIVATE cxx_std_20)
if(MSVC)
    target_compile_options(task05 PRIVATE /W4)
else()
    target_compile_options(task05 PRIVATE -Wall -Wextra -Wpedantic)
endif()
