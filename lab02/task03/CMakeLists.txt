cmake_minimum_required(VERSION 3.20)
project(task03 LANGUAGES CXX)
add_executable(task03 main.cpp)
target_compile_features(task03 PRIVATE cxx_std_20)
if(MSVC)
    target_compile_options(task03 PRIVATE /W4)
else()
    target_compile_options(task03 PRIVATE -Wall -Wextra -Wpedantic)
endif()
