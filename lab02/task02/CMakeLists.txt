cmake_minimum_required(VERSION 3.20)
project(task02 LANGUAGES CXX)
add_executable(task02 main.cpp)
target_compile_features(task02 PRIVATE cxx_std_20)
if(MSVC)
    target_compile_options(task02 PRIVATE /W4)
else()
    target_compile_options(task02 PRIVATE -Wall -Wextra -Wpedantic)
endif()
